# Unleash Score

A fullscreen, live scoreboard UI for **Supercode Unleash**.

Scaffold — the live, cross-device scoreboard lands in the first feature PR.

## Local development

```bash
npm install
npm run dev
```

## Deploy

Connected to Vercel via GitHub — merges to `main` ship to production.
